// Moved to packages/core (PRD §5's shared, framework-free domain package) —
// this file stays as a stable import path for the ~10 call sites across the
// app that already import from '@/lib/finance/debt'.
export * from '@pennyward/core'
